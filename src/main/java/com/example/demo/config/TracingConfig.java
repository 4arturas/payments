package com.example.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class TracingConfig {
    // Configuration for tracing is handled by Spring Boot auto-configuration
    // with the micrometer-tracing-bridge-otel dependency
}